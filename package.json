{
  "name": "mixpanel-import",
  "version": "2.2.2",
  "description": "stream and import to mixpanel from node.js",
  "main": "index.js",
  "keywords": [
    "mixpanel",
    "stream",
    "analytics",
    "tracking",
    "server",
    "CLI"
  ],
  "scripts": {
    "cli": "nodemon --inspect --ignore ./testData scratch.js --ignore ./logs foo.ndjson bar.ndjson --project 1234 --acct cliService --pass cliPass --secret cliSec --token cliToken --table lookuptableId --group grpKey --recordType event --compress --strict --logs --fixData --streamFormat strFrmt --streamSize 42 --region reg --recordsPerBatch 999999 --bytesPerBatch 9999",
    "dev": "nodemon --inspect --ignore ./testData scratch.js --ignore ./logs",
    "post": "npm publish",
    "import": "node index.js",
    "generate": "NUMEVENTS=9999 SINCEDAYS=30 node generateFakeData.js",
    "prune logs": "rm -rf ./logs/*",
    "test": "nodemon --inspect --ignore ./logs test.js"
  },
  "bin": {
    "mixpanel-import": "./index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ak--47/mixpanel-import"
  },
  "author": "AK",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ak--47/mixpanel-import/issues"
  },
  "homepage": "https://github.com/ak--47/mixpanel-import#readme",
  "dependencies": {
    "ak-tools": "^1.0.214",
    "chance": "^1.1.7",
    "dotenv": "^10.0.0",
    "got": "^11.8.6",
    "highland": "^2.13.5",
    "md5": "^2.3.0",
    "node-gzip": "^1.1.2",
    "papaparse": "^5.3.2",
    "stream-json": "^1.7.4",
    "underscore": "^1.13.2",
    "yargs": "^17.6.2"
  },
  "devDependencies": {
    "eslint": "^8.26.0",
    "nodemon": "^2.0.15"
  }
}
