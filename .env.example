# Google Cloud Configuration
# Copy this file to .env and customize as needed

# Project Configuration
PROJECT_ID=mixpanel-gtm-training
SERVICE_NAME=etl-lte
REGION=us-central1

# Cloud Run Resource Configuration
MEMORY=8Gi
CPU=4
TIMEOUT=3600
MAX_INSTANCES=10
MIN_INSTANCES=0

# Access Control
# Set to "true" to allow public access without authentication
# Set to "false" for authenticated access only (recommended)
ALLOW_UNAUTHENTICATED=false

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your values
# 3. Source before deploy: source .env && ./deploy.sh
# 4. Or set inline: PROJECT_ID=my-project SERVICE_NAME=my-service ./deploy.sh